export const Loader = () => {

    return (
        <section role="status" className="animate-pulse flex-grow flex items-center justify-center h-full overflow-y-auto">
            <p>Loading ...</p>
        </section>
    );
};